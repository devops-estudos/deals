apiVersion: traefik.io/v1alpha1
kind: TraefikService
metadata:
  name: deals-traefik-service
spec:
  weighted:
    services:
      - name: deals-current
        port: service
      - name: deals-new
        port: service
        
